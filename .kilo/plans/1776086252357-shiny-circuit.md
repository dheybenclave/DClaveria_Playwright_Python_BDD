# Plan: Fix Existing Failed Test Scripts

## Issue Summary
16 tests failing - two categories of problems:
1. **Missing Step Definitions** - Security tests (SQL injection, XSS, authentication) not implemented
2. **Missing Page Object Methods** - CommonPage and ProductsPage missing required methods

## Test Run Results (Previous Session)

```
=========== 16 failed, 28 passed, 5 deselected in 146.11s ===========
```

### Category 1: Step Definition Not Found (11 tests)
| Test | Feature File | Missing Step |
|------|-------------|--------------|
| SEC01 | sql_injection_test.feature | SQL injection on login username |
| SEC02 | sql_injection_test.feature | SQL injection on login password |
| SEC03 | sql_injection_test.feature | SQL injection in search field |
| SEC10 | xss_test.feature | XSS in search field |
| SEC11 | xss_test.feature | Stored XSS in contact form |
| SEC12 | xss_test.feature | XSS in registration name field |
| SEC14 | xss_test.feature | Reflected XSS in search |
| SEC20 | authentication_test.feature | Login with weak password |
| SEC22 | authentication_test.feature | Login brute force protection |

### Category 2: Missing Page Object Methods (6 tests)
| Test | Page Object | Missing Method |
|------|-------------|----------------|
| SEC21 | CommonPage | `get_page_content` |
| SEC23 | CommonPage | `wait_for_page_load` |
| SEC24 | CommonPage | `navigate_to_account_settings` |
| SEC25 | CommonPage | `get_page_content` |
| SEC05 | CommonPage | `enter_contact_name` |
| SEC13 | ProductsPage | `click_first_product` |

---

## Implementation Options

### Option A: Skip Incomplete Tests (Recommended)
Add `@skip` tag to security feature files to disable incomplete tests.
- Quickest solution - no code changes needed
- Keeps rest of test suite working
- Security tests can be implemented later

### Option B: Implement All Fixes
Add missing page object methods and step definitions.
- More work but completes the test suite
- Files to modify:
  - `src/pages/common_page.py` - Add 4 methods
  - `src/pages/products_page.py` - Add 1 method
  - Create step definition files for security tests